{
  "name": "abi-to-zod",
  "version": "0.0.0",
  "description": "Generate zod schemas from Ethereum ABIs",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "bin": {
    "abi-to-zod": "./dist/cli.js"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    },
    "./codegen": {
      "types": "./dist/codegen.d.ts",
      "import": "./dist/codegen.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "postbuild": "chmod +x dist/cli.js",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "regenerate:golden": "npm run build && node scripts/regenerate-golden.mjs"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yahgwai/abi-to-zod.git"
  },
  "dependencies": {
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@arbitrum/nitro-contracts": "^3.2.0",
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3",
    "vitest": "^3.2.4"
  }
}
